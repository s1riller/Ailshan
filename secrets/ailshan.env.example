# Скопируйте в /opt/ailshan/secrets/ailshan.env (chmod 600). Этот файл читает
# только контейнер; в git и в образ он не попадает.
# Значение печатает `scripts/supabase.sh keys` (свой Supabase на сервере).
SUPABASE_SERVICE_ROLE_KEY=<SERVICE_ROLE_KEY>
